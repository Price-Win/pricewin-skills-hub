
# skill-bundled deps installed on dev machines
skills/*/node_modules/

# any nested node_modules (e.g. legacy/)
**/node_modules/

# local ClawHub publish token (never commit)
.clawhub.token

# local env / secrets (never commit)
.env
.env.*
!.env.example

# superpowers tool session artifacts
.superpowers/
